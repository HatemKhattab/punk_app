<%-if @arr.present? && @arr.size > 0%>
  <table>
    <tr>
      <th>Name</th>
      <th>ABV</th>
    </tr>
    <%-@arr.each do |row|%>
      <tr>
        <th><%= link_to row["name"], home_path(row["id"])%></th>
        <th><%= link_to row["abv"]%></th>
      </tr>
    <%-end%>
  </tabel>
<%-end%> 
