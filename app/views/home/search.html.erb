
<%-if @view.present?%>
  <table class="table table-striped">
    <thead class="thead-dark">
      <tr>
        <th>Name</th>
        <th>ABV</th>
      </tr>
    </thead>
    <tbody class="table-striped">
    <%-@view.each do |row|%>
      <tr>
        <td><%= link_to row["name"], home_path(row["id"]) %></th>
        <td><%= row["abv"] %></th>
      </tr>
    <%-end%>
    </tbody>
  </tabel>
  <%= will_paginate @view %>
<%-end%>
</br>


