
<%-if @view.present?%>
  <table>
    <tr>
      <th>Name</th>
      <th>ABV</th>
    </tr>
    <%-@view.each do |row|%>
      <tr>
        <th><%= link_to row["name"], home_path(row["id"]) %></th>
        <th><%= row["abv"] %></th>
      </tr>
    <%-end%>
  </tabel>
  <%= will_paginate @view %>
<%-end%>
</br>


